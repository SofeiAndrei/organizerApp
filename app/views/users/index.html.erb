<% provide(:title, "All Users") %>
<h1>All Users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
  <!--  e echivalent cu < %= render @users %>   -->
</ul>

<%= will_paginate %>