<%= form_with(model: current_user.sent_friendship_requests.build, remote: true) do |f| %>
  <div>
    <%= hidden_field_tag :receiver_id, @user.id %>
  </div>
  <%= f.submit "Add Friend", class: 'btn btn-primary button-friendship-action' %>
<% end %>